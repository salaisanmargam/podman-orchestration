apiVersion: apps/v1
kind: Deployment
metadata:
  name: mul-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mul-service
  template:
    metadata:
      labels:
        app: mul-service
    spec:
      containers:
        - name: mul-service
          image: mul-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 5000

---
apiVersion: v1
kind: Service
metadata:
  name: mul-service
spec:
  type: NodePort
  selector:
    app: mul-service
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30019